# URL of the deployed RAG agent API (no trailing slash).
# When empty, the chat widget falls back to a local mock reply.
# Local dev: http://localhost:8000
# Production: https://your-render-service.onrender.com
VITE_CHAT_API_URL=
